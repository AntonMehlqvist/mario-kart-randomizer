<script>
	import { webVitals } from '../scripts/webVitals';
	console.log('Hej');
	const analyticsId = import.meta.env.PUBLIC_VERCEL_ANALYTICS_ID;

	webVitals({
		path: window.location.pathname,
		analyticsId,
	});
</script>
